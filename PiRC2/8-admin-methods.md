## 8. Admin Methods

### 8.1 `upgrade(new_wasm_hash)`

Upgrades the contract WASM. Requires `admin` authorization.

**Events:** `upgrade`

### 8.2 `version() -> u32`

Returns the current contract version (currently 3).

Next: [`9-Subscription-Setup-Guide`](9-subscription-setup-guide.md)
